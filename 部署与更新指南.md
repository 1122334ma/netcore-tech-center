# 磊科产品技术中心 - 部署与更新指南

## 一、项目结构

```
netcore-tech-center/
├── .vitepress/
│   ├── config.ts          # 站点配置（导航栏、侧边栏、搜索等）
│   └── theme/
│       ├── index.ts       # 主题入口
│       └── custom.css     # 自定义样式（颜色、字体等）
├── 01-产品中心/            # 产品中心栏目
├── 02-快速配置/            # 快速配置栏目
├── 03-高级功能/            # 高级功能栏目
├── 04-故障排查/            # 故障排查栏目
├── 05-常见问题FAQ.md       # FAQ 页面
├── index.md               # 首页
└── package.json            # 项目配置
```

## 二、日常更新内容

### 添加/修改文章

1. 找到对应栏目的文件夹（如 `01-产品中心/`）
2. 用任何文本编辑器（VS Code、记事本等）打开 `.md` 文件
3. 用 Markdown 语法编辑内容
4. 保存后重新构建即可上线

### 新增文章

1. 在对应栏目文件夹下新建 `.md` 文件
2. 在 `.vitepress/config.ts` 的 `sidebar` 中添加链接
3. 重新构建

### Markdown 基本语法

```markdown
# 一级标题
## 二级标题
### 三级标题

正文段落

- 列表项1
- 列表项2

| 列1 | 列2 |
|-----|-----|
| A   | B   |

> 引用提示文字

[链接文字](其他页面.md)
```

## 三、免费部署方案（三选一）

### 方案 A：Vercel 部署（推荐）

**优点**：免费、自动构建、全球 CDN、自定义域名

1. 注册 GitHub 账号（https://github.com）
2. 注册 Vercel 账号（https://vercel.com，可用 GitHub 登录）
3. 将 `netcore-tech-center` 文件夹上传到 GitHub 仓库
4. 在 Vercel 中导入该仓库
5. 配置：
   - Build Command: `vitepress build`
   - Output Directory: `.vitepress/dist`
6. 点击 Deploy，等待 1-2 分钟即可获得公开链接
7. 以后每次更新代码，Vercel 自动重新构建发布

### 方案 B：Cloudflare Pages 部署

**优点**：免费、不限流量、国内访问较快

1. 注册 Cloudflare 账号（https://dash.cloudflare.com）
2. 进入 Pages > Create a project
3. 连接 GitHub 仓库
4. 配置：
   - Build Command: `npm run build`
   - Build Output Directory: `.vitepress/dist`
5. 部署完成后获得 `xxx.pages.dev` 域名

### 方案 C：本地构建 + 手动上传

**优点**：不依赖第三方平台

1. 本地运行 `npm run build`
2. 将 `.vitepress/dist` 目录上传到任意静态网站托管服务
3. 如：阿里云 OSS、腾讯云 COS 静态网站托管等

## 四、本地预览

```bash
# 安装依赖（首次）
npm install

# 本地预览
npm run dev

# 构建
npm run build

# 预览构建结果
npm run preview
```

## 五、客户使用方式

部署完成后，你只需要把链接发给客户：
- 客户用手机或电脑浏览器打开即可
- 支持搜索功能（右上角搜索框）
- 支持暗色/亮色主题切换
- 手机自适应布局

## 六、自定义品牌

### 修改主题颜色

编辑 `.vitepress/theme/custom.css`，修改 `--vp-c-brand-1` 等变量。

### 添加 Logo

将 logo 图片放到 `public/` 目录，在 `config.ts` 中取消注释 `logo: '/logo.png'`。

### 修改导航栏

编辑 `.vitepress/config.ts` 中的 `nav` 配置项。

### 修改侧边栏

编辑 `.vitepress/config.ts` 中的 `sidebar` 配置项。

## 七、常见问题

**Q: 构建失败怎么办？**
A: 检查 Node.js 版本（需 18+），运行 `npm install` 确保依赖安装。

**Q: 搜索功能不工作？**
A: VitePress 内置本地搜索，构建后自动生效，无需额外配置。

**Q: 能加图片吗？**
A: 能。把图片放到 `public/` 目录，在 Markdown 中用 `![描述](/图片名.png)` 引用。

**Q: 如何统计访问量？**
A: 可在 `config.ts` 的 `head` 中添加百度统计或 Google Analytics 代码。
